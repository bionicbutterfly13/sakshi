[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pysakshi"
version = "0.9.0"
description = "A metacognitive runtime for Python agents: the Witness pattern."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Dr. Mani Saint-Victor" }]
maintainers = [{ name = "Dr. Mani Saint-Victor" }]
keywords = [
    "metacognition",
    "cognitive-architecture",
    "agents",
    "active-inference",
    "monitor-assess-control",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.0",
    "typing-extensions>=4.0",
]

[project.urls]
Homepage = "https://github.com/bionicbutterfly13/sakshi"
Source = "https://github.com/bionicbutterfly13/sakshi"
Issues = "https://github.com/bionicbutterfly13/sakshi/issues"
Changelog = "https://github.com/bionicbutterfly13/sakshi/blob/main/CHANGELOG.md"
Provenance = "https://github.com/bionicbutterfly13/sakshi/blob/main/INSPIRATION.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.1.0",
]

[tool.setuptools]
packages = [
    "sakshi",
    "sakshi.models",
    "sakshi.cycle",
    "sakshi.goals",
    "sakshi.plans",
    "sakshi.meta",
    "sakshi.interpret",
    "sakshi.registries",
    "sakshi.intake",
    "sakshi.world",
]

[tool.setuptools.package-data]
sakshi = ["py.typed"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
